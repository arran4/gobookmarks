{{ define "description/reset-password" }}
{{ .Command.Name }} updates a specific user's password in the configured SQL backend.
Provide `--user` and a strong `--password`; the command validates the length before writing the hash.
Use it to recover access without touching the web UI.
{{ end }}

{{ template "partials/command" . }}
